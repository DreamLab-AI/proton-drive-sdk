# macOS
.DS_Store

# Docs
__pycache__
docs/build
/public
reference/client/js/public

# JS
node_modules
.eslintcache
tsconfig.tsbuildinfo
dist

# JS CLI
reference/cli/release
auth.txt
auth-session.json
cache*.sqlite
events.json
events.lock
clientUid.json
*.log
*.bun-build
config.json
*.map

# IDEs
.vs
.vscode
.idea
*.swp

# Tests
tests/storage
tests/test-results

# Rust
rust/target
**/*.rs.bk
Cargo.lock.bak

# Agent metadata (not part of source tree)
.agentic-qe/
.claude-flow/
.swarm/
**/.claude-flow/

# Local-only secrets for the Rust port (see scripts/README.md)
rust/.env
rust/fixtures/auth/*
!rust/fixtures/auth/.gitkeep

# Credentials / secrets (defensive — none of these belong in the repo).
# pdtui keeps its live session in the OS keyring; configure-session.sh writes
# session.json under $XDG_CONFIG_HOME/pdtui and probes write to $TMPDIR, all
# outside the tree. These patterns guard against an accidental repo-local copy.
.env
.env.*
session.json
*.pem
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_rsa.pub
.claude/
